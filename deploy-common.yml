version: "3.7"
networks:
  backend:
volumes:
  pgsql_data:
  mongo_config:
  mongo_data:
  rabbit_data:
  xhgui_webroot_share:
  nginx_run:
  pgadmin_data:
  insight_data:
  zookeeper_data:
  zookeeper_datalog:
  zookeeper_log:
