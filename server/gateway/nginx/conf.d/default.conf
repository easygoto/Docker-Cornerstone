upstream backServer {
    server nginx_1:80;
    server nginx_2:80;
    server nginx_3:80;
}

server {
    listen       80;
    server_name  localhost;

    access_log  /var/log/nginx/host.access.log  main;

    location / {
        proxy_pass http://backServer;
    }
}