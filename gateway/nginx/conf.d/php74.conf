upstream PHP74Server {
    server nginx74_1:80;
    server nginx74_2:80;
}

server {
    listen       80;
    server_name  php74host;

    access_log  /var/log/nginx/php74.host.access.log  main;

    location / {
        proxy_pass http://PHP74Server;
        proxy_set_header X-Forwarded-For $remote_addr;
    }
}
