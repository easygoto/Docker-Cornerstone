version: "3.7"
services:
  nginx5_1:
    image: nginx
    networks:
      - backend
    depends_on:
      - phpx5_1
    volumes:
      - $SYSTEM5_1_HTML_DIR:/var/www/html
      - $SYSTEM5_1_NGINX_CONFIG_FILE:/etc/nginx/nginx.conf
      - $SYSTEM5_1_NGINX_CONFIG_DIR:/etc/nginx/conf.d
      - $SYSTEM5_1_NGINX_LOG_DIR:/var/log/nginx
    privileged: true

  phpx5_1:
    image: registry.cn-hangzhou.aliyuncs.com/treelink/php:php5-fpm-xdebug
    networks:
      - backend
    volumes:
      - $SYSTEM5_1_HTML_DIR:/var/www/html:rw
      - $SYSTEM5_1_PHP_INI_FILE:/usr/local/etc/php/php.ini:ro
    privileged: true
